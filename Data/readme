put unconverted text files here.

Then start the "Data_Converter.py" skript. This will iterate through all insertet files. They will be converted to csv format and put
in the converted folder. The .txt files will be moved to the raw folder